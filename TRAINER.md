# Trainer 练习工作区

当前开发版本：v0.6.0。默认乐谱跟练来源，另可切换随机练习 / 自由弹奏。

## 随机练习与自由弹奏（2026-09-07）

Trainer 分为「乐谱跟练」「随机练习」「自由弹奏」。随机练习生成标准 ScoreDocument，并复用乐谱跟练的逐音 / 时间轴识别、谱面反馈、小节范围、循环、示范与成绩；两种来源使用独立控制器，切换时暂停并保留各自进度。

- 预设：基础认音、旋律视奏、混合节奏、三和弦练习。可折叠出题设置，音名提示支持隐藏 / C / C4。
- 音域：A0–C8 独立上下限；单音可选白键、黑键或全部。大 / 小三和弦允许黑键，全部组成音必须在范围内。
- 节奏：七种现有拍号、2 / 4 / 8 / 16 小节，五种时值可多选。严格填满每小节，不补入未选时值；条件不成立时提示错误，保留当前练习。
- 可选少量休止符、以相邻音为主的单音变化（优先四个半音以内，非严格级进）。每小节第一项保证有音，休止符为概率生成。
- 修改参数只改变待生成条件；点击「生成新题」才替换练习。「重新开始」和循环仍练同一份谱。
- 「送到 Editor」复制当前随机谱与 BPM；已有内容时确认替换，支持撤销恢复。只有在 Editor 手动保存才覆盖本地草稿。
- 自由弹奏提供即时谱面、音名 / 唱名及已有的大 / 小三和弦识别。全部松键后保留最近试音，可清除；同组采用最后一次新按键时的同时持音，不累加先后独奏音。没有播放或计分控件。

随机参数、生成谱、音名提示与最近试音仅在当前页面会话中保留，刷新后重置；需要保留题目时送入 Editor 保存或导出。

## 时间轴跟弹（新增）

在「练习模式」选择「时间轴跟弹」，点击「开始跟弹」。预备一小节后，以当前 BPM 自动推进；分母为八分音符的拍号按八分音符显示预备拍数。预备期间不计分，暂停后按原位置继续；更改 BPM 会暂停，继续后使用新速度。计时包括预备时间。

跟弹不等待弹对或松键，休止符按实际时值经过且暂不计分。播放轴前后各半拍构成固定识别带；原始事件的时值范围进入识别带即可开始触发，离开轴后只要仍与识别带相交也可触发，完全离开后才结算。事件严格按乐谱顺序识别，不能越过尚未结算的前一事件。单音触发目标音即可命中，和弦需曾同时按住全部目标音，允许包含额外音。按错后可在识别期间补弹正确，额外音只作即时警告而不扣分；每个事件最终只结算为「正确」或「未通过」。连续同音及连续和弦都需要在各自识别期间重新触发目标音，跨小节延音仍只按原始事件判断一次。当前不评价精确起音偏差或持续按键时长。

时间轴模式播放轴保持可视区左侧约 20%，固定识别带围绕播放轴显示，谱面从右向左经过该区域；短谱、末尾与窗口变化也保持锚定。正式音符与 Editor 一样在各自时值格内居中，识别带与音符中心位置相互独立。播放跟随使用浏览器绘制帧更新，Editor 的布局与中央安全区跟随保持原样。

两种模式均显示当前实奏的空心音符，可与目标直接比较音高；松键后消失。时间轴模式将实奏画在轴旁，逐音模式画在目标旁，升号与加线使用共享谱表几何。固定识别带使用浅背景与边框提示；识别期间命中后目标立即显示正确色，结算后以正确色加勾或警告色加叉保留结果。尚未命中时，仅非目标实奏音显示警告色。

BPM 同时控制跟弹和示范。示范播放 / 定位不修改练习位置；开始跟弹后谱面回到练习时间轴。

## 逐音模式使用

1. 在 Editor 编辑后点击「去练习」，或在 Trainer 点击「载入已保存乐谱」。前者包含未保存内容，后者读取本地最后一次保存；均深拷贝为独立练习副本。
2. 选择整曲或起止小节。范围包含与这些小节重叠的事件；进入范围时已开始的延音也作为完整音高目标，跨小节显示片段不重复计分。
3. 可先听示范：回放仅包含选定小节，边界处时值被裁剪，保留区间内休止符。进度、回到开头、定位当前目标和 30–240 BPM（±10）与 Editor 保持相同设计。
4. 点击开始练习。单音或和弦必须匹配全部目标音高，全部松键才推进；和弦逐音分开弹奏不能通过。错音、不完整和弦均需松键重试。自由速度模式不考核时值，休止符自动跳过。
5. 可暂停、重新开始或点击范围内的谱面音符重新计分；点击延音片段定位其原始事件。循环在完成后重新开始并保留上一轮结果。

练习与示范回放互斥。切换工作区、练习来源或页面进入后台会暂停。按住实奏琴键时禁用会切换练习位置 / 来源的操作。所有键松开后才能开始；重复松键不计错。

## 成绩与状态

- 时间轴跟弹：按稳定事件 ID 保存本轮结果，显示已结算、正确、未通过和通过率；未结算事件不进入通过率分母。
- 时间轴暂停保留当前窗口命中状态与历史结果；重新开始、改变范围、换谱或选择新起点会清空本轮结果；循环新一轮清空谱面结果并保留上一轮摘要。
- 完成：本轮已完成目标 / 本轮目标数。中途选择起点后只统计该项至范围末尾。
- 首次正确率：首次尝试正确的已完成目标 / 已完成目标，尚未完成时显示 0%。
- 错误次数：每组从按键到全部松开的失败尝试计一次；不按错误音个数累加。
- 用时：仅累计练习运行时间，暂停、示范和后台时间不计入。
- 已完成轮次与上一轮结果用于本次会话；改变范围会清空轮次和上一轮结果。

## 代码边界

- `scorePractice.ts`：独立可测试的目标、输入来源持有状态、匹配、推进、区间和统计。
- `ScoreTrainer.tsx`：文档来源、跟练控制、共享 ScoreStaff、区间示范及状态显示。
- `randomScore.ts`：受约束随机出题与小节填充。`RandomTrainer.tsx`：出题草稿、预设与 Editor 交接。
- `FreePlay.tsx`：即时谱面与最近试音。
- `PracticeTransport.tsx`：乐谱副本共用回放界面，复用 ScoreTransport。
- `App.tsx`：Editor 交接与实奏输入路由；playback 在入口单独处理，不进入练习计分。
- ScoreDocument 仍为 version 1；不写入练习配置、成绩、琴键状态或坐标。

## 验证与限制

`npm run build`、`npm run lint`、`npm test`。当前 39 项测试（新增随机音域 / 音符池 / 和弦约束、精确小节填充、无效条件、预设与控制器隔离）覆盖副本隔离、同音源释放、采样锚点、完整和弦、额外音、重复松键、重试计分、休止符、固定识别带提前／延后输入、严格顺序、连续同音新触键、即时警告、跨小节共享结果、区间裁剪、循环、暂停状态、谱面反馈及单一 RAF 调度。

浏览器验收覆盖当前乐谱交接、本地载入、单音重试、连续同音、成绩、小节循环及示范播放。随机页面浏览器已验证出题、无效音域保护、逐音推进和计分、来源切换、自由试音与清除；Editor 交接确认框导致自动化连接超时，交接 / 撤销浏览器验收待人工完成。USB / Bluetooth MIDI 硬件尚未连接实测。

Technical Debt：时间轴当前只按固定识别带内的事件顺序评价音高集合，尚无精确起音 / 时长评分、练习历史或错题库。练习副本和成绩只在当前页面会话中保存，刷新后需重新载入。主要谱面排版与前台回放调度限制沿用 Editor。旧随机控制器与设置数据结构暂保留兼容，新随机页面不读取旧出题配置；后续可清理。实奏音频与键盘高亮均按输入来源持有同音。
